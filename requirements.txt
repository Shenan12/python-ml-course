# On Linux (e.g. Streamlit Community Cloud), plain `pip install torch` pulls
# the multi-gigabyte CUDA build and blows the deployment's resource limits.
# This extra index makes pip prefer the small CPU-only wheels instead.
# (Harmless on Windows/Mac.)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core app
streamlit>=1.49
streamlit-ace>=0.1.1

# Data & plotting
numpy>=2.0
pandas>=2.2,<3        # pycox's dependency chain requires pandas < 3
pyarrow>=14           # parquet support for pandas (Section 4's dataset)
matplotlib>=3.9
plotly>=5.24

# Machine learning
scikit-learn>=1.5

# Big data (ALSO REQUIRES JAVA - see the README before installing)
pyspark>=4.0

# Deep learning & survival analysis (Sections 5-7)
torch>=2.4
torchtuples>=0.2.2
pycox>=0.3.0
lifelines>=0.29
